# Legacy Agent Entrypoint

This file exists only for compatibility with tools that still look for `AGENT.md`.

Use `AGENTS.md` for agent instructions and `AI_GUIDE.md` as the repository source of truth for implementation rules.
