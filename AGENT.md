# Legacy Agent Entrypoint

Read `AGENTS.md` first, then `AI_GUIDE.md`.

`AI_GUIDE.md` is the repository source of truth for implementation rules,
architecture, Docker runtime, and verification gates.

If this file conflicts with `AGENTS.md`, `AI_GUIDE.md`,
`template/fsd.rules.json`, or existing project patterns, follow the project
source-of-truth files.
